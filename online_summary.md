# 本輪討論總結

## 測試

- 為 `api/flask_app` + `core` 建立完整 pytest 測試套件，涵蓋 routes、services、models、core 各層，達到 91%+ 行覆蓋率（127 個測試）
- 測試環境跟正式/開發環境完全隔離，不會誤觸真實資料庫或外部 API

## LINE Bot 功能

- 使用者傳貼圖、圖片等非文字訊息時，現在會回覆提示訊息，不再靜默忽略

## 網頁前端（web_frontend）

- AI 回覆與錯誤訊息改成正確的對話泡泡樣式（原本 CSS 被覆蓋掉變成無樣式文字）
- 錯誤訊息改成留在對話紀錄裡的泡泡，不再是 3 秒後自動消失的提示
- 修正使用者訊息尾端多空白的顯示問題
- 移除有 bug 的固定秒數送出冷卻機制，改成單純依請求是否完成中判斷，回覆完可立即再送出

## 專案清理

- 移除完全沒被使用、已經壞掉的舊版 `web/` 目錄（LINE 舊版 SDK、遺留變數，跟現有架構無關）

## 正式環境部署

專案現在有實際上線的正式環境：

- **後端 API**：部署在 Render（Docker 容器、healthcheck、單一 worker 避免資料庫初始化競爭問題）
- **資料庫**：改用 Neon 提供的 Postgres（永久免費方案）
- **前端**：部署在 Cloudflare Workers（靜態網站）
- **跨網域登入**：修正前後端分屬不同網域時 LINE 登入 session cookie 失效的問題
- **CI**：GitHub Actions 在每次 push/PR 時自動跑測試把關

目前 API 與網頁前端都已實際上線並驗證可以互相溝通、AI 問答功能正常運作。
